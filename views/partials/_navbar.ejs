<div class="header">
    <a href="/" class="header__logo">BLOGsite</a>
    <nav class="header__navbar">
        <span> 
            <% if(user){ %>
                <%= "Hello, "+user.username+"!" %>
              <% } %>
        </span>
        <a href="/">Home</a>
        <a href="/users/profile">Profile</a>
        <a href="/new-blog">Write</a>
        <% if(user){ %>
            <a href="/logout">Logout</a>
          <% } else{ %>  
            <a href="/login">Login</a>
        <a href="/register">Register</a>
         <% } %>
    </nav>
</div>